# ESPHome Config — Claude Anweisungen

## Pflicht: Immer den esphome-project-expert Agent verwenden

Bei **jeder** Aufgabe in diesem Verzeichnis — YAML-Änderungen, Flashen, Debugging, neue Projekte — **muss** der `esphome-project-expert` Agent verwendet werden. Keine Ausnahmen, auch nicht bei kleinen Änderungen.

Beim Spawnen: relevanten Kontext aus der aktuellen Konversation mitgeben (IP-Adresse, was gerade gemacht wird, aktuelle Fehlermeldungen etc.).
